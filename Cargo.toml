[package]
name = "rowview"
version = "0.1.0"
edition = "2024"

[dependencies]
layout = "0.0.1"
rowview_macros = { path = "rowview_macros" }
# manyhow?

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
subdef ={ version = "*" }

[workspace]
members = ["rowview_macros"]
