[package]
name = "rowview"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "*"
proc-macro2 = { version = "1" }
strum = { version = "*", features = ["derive"] }
# manyhow?

[dev-dependencies]
subdef ={ version = "*", features = ["derive"] }