[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]
derive_more = { version = "2", features = ["from"] }
layout = "0.0.1"
rowview = { path = ".." }
scoped-panic-hook = "0.1"
serde = { version = "1", features = ["derive"] }
subdef = { version = "*" }
